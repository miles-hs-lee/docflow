// The default clickwrap NDA / agreement sentence. Used as the starting value in
// the data-room link-create form AND as the viewer-side fallback shown when a
// link enables the agreement gate without custom text — one source so the two
// surfaces never drift.
export const DEFAULT_AGREEMENT_TEXT =
  '본 문서를 열람함으로써 본인은 문서에 포함된 정보를 기밀로 유지하며, 사전 서면 동의 없이 제3자에게 공개하지 않을 것에 동의합니다.';
